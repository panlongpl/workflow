## 1. 评论内容构造

- [x] 1.1 调整单条评论发送内容构造，使 `buildSingleAnnotationPrompt()` 仅返回用户评论正文。
- [x] 1.2 调整历史批量发送内容构造，使 `buildBatchAnnotationPrompt()` 仅合并选中评论的用户评论正文，不附加任务标记、编号、路径、引用或上下文。
- [x] 1.3 保留发送内容 trim 和空内容失败路径，避免空评论写入终端。

## 2. 终端提交行为

- [x] 2.1 确认 `dispatchPrompt()` 在成功写入评论内容后统一补充 Enter 键输入。
- [x] 2.2 确保 Agent 不可用、连接不可用或内容为空时不会额外发送 Enter。

## 3. 回归与反馈

- [x] 3.1 确认评论抽屉“发送给 Agent”和保存后自动发送继续复用统一派发路径。
- [x] 3.2 确认历史单条发送和历史批量发送继续复用统一派发路径，并保持成功/失败 toast 语义。
- [x] 3.3 确认评论保存语义、历史选择态清理和失败保留行为不受影响。

## 4. 验证

- [x] 4.1 通过静态检查或语法检查验证前端脚本无语法错误。
- [x] 4.2 验证单条发送写入终端的内容仅为评论正文且自动提交。
- [x] 4.3 验证批量发送写入终端的内容仅为多条评论正文集合且自动提交。
