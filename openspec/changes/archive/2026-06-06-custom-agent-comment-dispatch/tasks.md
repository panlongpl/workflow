## 1. 自定义 Agent 派发能力

- [x] 1.1 调整终端控制器的可派发 Agent 判断，使运行中且连接正常的 custom 会话可以通过 `canDispatchToAgent()`。
- [x] 1.2 保留 no-session、starting、closed、socket-not-open、empty-prompt 等失败路径，不因 custom 放行而跳过基础校验。
- [x] 1.3 确保评论抽屉、自动发送、历史单条发送、历史批量发送继续复用 `dispatchPrompt()`，不新增重复发送逻辑。

## 2. 自定义 Agent 状态同步

- [x] 2.1 在启动 custom 会话时保存自定义命令或命令摘要，用于状态展示。
- [x] 2.2 更新 `getDispatchStatus()`，使可派发 custom 会话显示绿色可用状态和自定义 Agent 名称或命令摘要。
- [x] 2.3 在 custom 启动成功、断开、错误、结束、重启时保持顶部 Agent 状态与真实派发能力同步。

## 3. 展示与反馈

- [x] 3.1 确保顶部状态在 custom 可用时显示“可用”语义，且 title 或 aria-label 包含可理解的自定义 Agent 信息。
- [x] 3.2 如自定义命令过长，保持顶部状态文本可截断且完整信息仍可通过 title 查看。
- [x] 3.3 确保 custom 发送失败时仍使用现有连接不可用、会话不可用或发送失败文案。

## 4. 验证

- [x] 4.1 验证 custom 会话启动并运行后，评论抽屉“发送给 Agent”可以写入该会话。
- [x] 4.2 验证自动发送开启时，保存评论后可以派发到运行中的 custom 会话。
- [x] 4.3 验证历史单条发送和批量发送可以派发到运行中的 custom 会话。
- [x] 4.4 验证 custom 启动中、断开或连接不可用时不会显示为可用，也不会盲目写入。
- [x] 4.5 验证 Codex / Claude 的既有派发能力和顶部状态不受影响。
- [x] 4.6 运行相关前端语法检查或 smoke test，确认改动未破坏现有评论派发与状态展示。
