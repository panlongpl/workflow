## 1. 全局配置与状态

- [x] 1.1 为“保存评论后自动发送给 Agent”增加本地持久化配置键与读写辅助逻辑。
- [x] 1.2 在页面初始化时恢复该配置，并在 Markdown 相关控制区展示对应开关。
- [x] 1.3 约定默认值为关闭，并在切换配置时即时更新前端状态与本地存储。
- [x] 1.4 明确该配置不放入评论抽屉内部，避免被误解为单条评论字段。
- [x] 1.5 为评论抽屉和历史发送动作增加 pending 状态，防止重复点击导致重复保存或重复发送。
- [x] 1.6 为新增全局开关补充明确 label、可聚焦状态和与现有控制区一致的视觉样式。

## 2. 评论抽屉发送能力

- [x] 2.1 在评论抽屉中新增“发送给 Agent”按钮，并将现有“保存”文案明确为“仅保存”或等价语义。
- [x] 2.2 将评论保存逻辑拆分为“保存到本地”和“尝试派发到 Agent”两个清晰步骤。
- [x] 2.3 实现“仅保存”行为：始终保存；若全局自动发送开启，则在保存成功后自动发送。
- [x] 2.4 实现“发送给 Agent”行为：始终先保存，再强制发送，不受全局配置影响。
- [x] 2.5 为“已保存未发送”“已保存并发送”“保存失败”“发送失败”分别提供清晰反馈。
- [x] 2.6 保证“发送给 Agent”失败不会覆盖或回滚已经成功的本地保存结果。
- [x] 2.7 点击“仅保存”或“发送给 Agent”期间禁用相关按钮，动作结束后恢复。
- [x] 2.8 发送成功后不强制切换到 AI 终端，避免打断连续阅读；可选择在 toast 中提供“查看终端”入口。

## 3. Prompt 组装与内容控制

- [x] 3.1 定义单条评论 prompt 模板，包含任务标记、工作目录、文档路径、引用文本、评论内容、前后文和处理指引。
- [x] 3.2 定义历史批量 prompt 模板，按编号合并多条注释并说明统一分析和按优先级处理。
- [x] 3.3 集中配置批量数量上限、quote/note/context 裁剪长度和截断标记。
- [x] 3.4 确保 prompt 组装逻辑对空字段、超长字段、多文档、多根目录历史记录都有可预期输出。

## 4. 终端派发桥接

- [x] 4.1 为终端控制器补充可供评论系统调用的派发能力，例如发送 prompt 与检测当前会话可用性。
- [x] 4.2 在没有可用运行中 Agent 会话时返回明确失败结果，而不是静默吞掉发送动作。
- [x] 4.3 为不具备可靠任务派发语义的会话类型返回“不可派发”状态，避免盲发到普通 shell 或未知自定义命令。
- [x] 4.4 第一版限定明确支持的 codex / claude 运行中会话可派发，custom 默认不可派发。
- [x] 4.5 发送 prompt 时补齐必要的提交换行，确保 Agent 能收到一次完整输入。

## 5. 历史注释重派发

- [x] 5.1 在历史注释“按文档”视图中为每条注释增加勾选态，并维护选中集合。
- [x] 5.2 在历史注释页头增加“发送选中项给 Agent”入口，并展示当前选中数量。
- [x] 5.3 为单条历史注释增加“发送”入口，支持直接重派发单条评论。
- [x] 5.4 将批量发送实现为单次合并 prompt，而不是逐条向 PTY 连续写入。
- [x] 5.5 保持历史注释“单行文本”模式为轻量复制视图，不在该模式中引入勾选发送交互。
- [x] 5.6 为历史批量发送增加合理的单次发送上限或内容裁剪策略，避免 prompt 过长。
- [x] 5.7 离开历史页、切换到单行文本模式或历史数据重新加载时，按设计规则清理无效选中项。
- [x] 5.8 批量发送成功后清空已发送选中项；批量发送失败时保留选中项以便重试。

## 6. 反馈、样式与可访问性

- [x] 6.1 补齐保存成功、保存失败、发送成功、发送失败、不可派发、未选择、超出上限等 toast 文案。
- [x] 6.2 为新增按钮、开关、复选框补充与现有 UI 一致的样式。
- [x] 6.3 保证新增开关和复选框可通过键盘操作，并具备清晰 label。
- [x] 6.4 保持发送成功后不强制跳转终端的阅读体验。

## 7. 验证

- [x] 7.1 验证全局自动发送关闭时，点击“仅保存”只会保存到本地而不会发送。
- [x] 7.2 验证全局自动发送开启时，点击“仅保存”会在保存成功后自动发送。
- [x] 7.3 验证点击“发送给 Agent”时，无论全局配置如何都会先保存再发送。
- [x] 7.4 验证没有运行中 Agent 会话时，评论保存成功但发送失败不会丢失本地记录。
- [x] 7.5 验证历史注释可以选择部分评论并一键发送给 Agent。
- [x] 7.6 验证自定义或未知会话类型不会被错误识别为可派发 Agent。
- [x] 7.7 验证历史批量发送在超出单次限制时会给出清晰提示或采用可预期裁剪策略。
- [x] 7.8 验证重复点击不会导致重复保存或重复发送。
- [x] 7.9 验证离开历史页、切换历史展示模式、重新加载历史数据时选择态符合设计。
- [x] 7.10 运行相关前端语法检查或 smoke test，确认新增交互未破坏现有 Markdown 浏览、评论保存与历史注释功能。
