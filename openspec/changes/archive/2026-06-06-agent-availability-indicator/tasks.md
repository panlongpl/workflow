## 1. 顶部状态 UI

- [x] 1.1 在页面顶部工具栏中新增 Agent 可用性状态容器，位置适配截图中顶部空白区域。
- [x] 1.2 为状态容器实现绿色可用圆点、Agent 名称、不可用/启动中/连接不可用等文案展示。
- [x] 1.3 为状态容器补充 `aria-label` 或等价可访问文本，确保状态不只依赖颜色表达。

## 2. 终端状态来源

- [x] 2.1 在终端控制器中提供当前派发可用性快照，复用现有 `canDispatchToAgent()` 判断。
- [x] 2.2 在 Agent 启动、启动成功、会话结束、连接断开、连接错误、重启和关闭时触发状态刷新通知。
- [x] 2.3 确保 custom 或未知会话类型继续被识别为不可派发，不显示为绿色可用。

## 3. 应用层集成

- [x] 3.1 在 `src/app.js` 中接收终端状态变化，并统一渲染顶部 Agent 状态。
- [x] 3.2 在页面初始化、切换模块、启动/关闭 Agent 后保证状态展示与实际派发能力一致。
- [x] 3.3 保持评论“仅保存”“发送给 Agent”和历史注释发送逻辑仍通过终端控制器做真实校验，不依赖顶部展示结果跳过校验。

## 4. 样式与响应式

- [x] 4.1 增加 Agent 状态 pill、状态圆点和不同状态 tone 的样式。
- [x] 4.2 在窄屏或顶部按钮较多时保持布局不挤压核心操作，必要时允许换行或缩短文案。
- [x] 4.3 保持新增状态样式与现有顶部按钮、开关视觉风格一致。

## 5. 验证

- [x] 5.1 验证无 Agent 会话时顶部显示不可用状态且没有绿色可用圆点。
- [x] 5.2 验证 Codex 或 Claude 会话运行且连接正常时顶部显示绿色圆点和对应 Agent 名称。
- [x] 5.3 验证 Agent 启动中、断开、结束或连接错误时顶部状态及时更新为非可用。
- [x] 5.4 验证 custom 会话不会被显示为可派发 Agent。
- [x] 5.5 验证顶部状态不改变评论保存、强制发送和历史批量发送的现有语义。
- [x] 5.6 运行相关前端语法检查或 smoke test，确认新增状态展示未破坏现有页面交互。
